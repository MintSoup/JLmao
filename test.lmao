let primes = 0;
func isPrime(a){
    for(let i = 2; i <= sqrt(a); i = i + 1){
        if(a % i == 0) return false;
    }
    return true;
}
let primes = 0;
let t = clock();
for(let i = 2; primes < 100000; i = i + 1){
    if(isPrime(i)){
        primes = primes + 1;
        print(istr(i));
    }
}

print(istr(clock() - t) + "ms taken to complete");

